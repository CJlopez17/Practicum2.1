import 'package:flutter/material.dart';
import 'package:practicum_final/widgets/calendarView.dart';

class CalendarVr extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: calendarView(),
    );
  }
}